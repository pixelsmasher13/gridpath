# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
tsconfig.tsbuildinfo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Workspace files (don't belong in source dirs)
*.code-workspace

# Env files (only .env.example is tracked)
.env
.env.*
!.env.example
*.env
!*.env.example

# Local tool configs (machine-specific)
.claude/

# Session transcripts / scratch
/2025-*.txt
/2026-*.txt

# Cloud credentials (never commit)
/heelix-*.pem
/*credentials*.txt

# Spreadsheet session data (per-user untitled drafts + change logs)
# Old paths landed in src-tauri/ before commit 2026-05-26; new code
# anchors these inside app_data_dir/untitled_sessions/.
*.changes.jsonl
src-tauri/untitled-*
